# ZCamp 尸群密度第二轮验收

日期：2026-08-13
范围：只验证“尸群密度第二轮实验”；未改 UI Bible、冰冻塔交互或其他 QA 项。

## 实现与自动化

- `src/core/content.ts`：普通敌人采用冻结的半量单体值；十波采用冻结的普通敌人数量；生成改为 16 个确定性脉冲锚点（0–39 秒，间隔 2.6 秒，组内微间隔 0–0.4 秒）；Boss 保持 39.5 秒最后出现。
- `tests/core/content.test.ts`：覆盖十波精确组成、普通敌人 HP/攻墙/金币/XP 预算 0.95–1.05、精英/Boss 数值不变、16 组脉冲、四个 10 秒区间覆盖、末组接近 39 秒与 Boss 39.5 秒。
- 阶段设计文档已同步标记“尸群密度第二轮实验”及普通敌人奖励/波次表。

验证结果：

- `npm run check`：6 个测试文件、50 个测试通过；typecheck 通过。
- `npm run build`：成功；仅有既有的单 chunk 超过 500 kB 提示，无编译错误。
- `git diff --check`：通过。

## 浏览器短段玩测

使用本地开发服，未执行长脚本；先 720×1280，再 360×640。

- 720×1280：开局布置一座机枪塔后，短段首采样 `enemyCount=7`；随后 5 次约 220ms 采样为 `1, 1, 0, 0, 0`，说明机枪塔能快速清理脉冲小群而非生成队列停滞。中段画面仍可见 2 只敌人，击杀 30，城墙 `92/100`，下一波倒计时约 24 秒；短观察窗内未见持续长空屏。
- 360×640：初始画面双资源、补给轨道、手牌和 5×3 格区均未发生明显重叠或裁切；布置机枪塔后的短采样 `enemyCount=5`，截图中同时可见多只敌人，城墙 `98/100`。
- 浏览器控制台：`error`/`warn` 均为空。

说明：本轮按验收指示只做 W1 分段采样，未强行推进 W2/W3；晚期峰值与性能风险由确定性波次/脉冲自动化回归覆盖，未因未完成长时浏览器采样而改动冻结数值。
