# ZCamp v0.8 资源 / 补给双进度验收

日期：2026-08-13
入口：本地 Vite `http://127.0.0.1:5175/`

## 自动化

- `npm run check`：6 个测试文件、50 个测试通过。
- `npm run build`：通过；Vite 保留既有的大 bundle warning。
- `git diff --check`：通过。
- 核心 selector 覆盖木材 / 金币支付 ratio 的 0、差 1、恰好 100%、超额 clamp；基础建造、真实升级最低费、工程墙动作；硬锁不返回伪支付进度；18 秒补给 progress / waiting / 终局停止；战术暂停冻结；主城、Lv.1/2/3 伐木场和永久木材加成产速。

## 浏览器观察

使用 720×1280 与 360×640 视口人工检查，截图为本次会话中的少量验收帧：

- 初始帧：轨道左侧只显示图形木材、`木材 120`、`+0.5/秒` 与金币；顶部仅保留波次 / 威胁，不重复金币；中央仅显示下一张牌和 18 秒补给条。
- 低木材帧：建造机枪塔与伐木场后，`木材 28`、`+1.5/秒`；火炮 65 与冰冻 55 的同资源液面高度不同；点击不足牌只显示准确的“还差 23 木材”，未进入选中态，也未吞牌。
- 暂停帧：`战术暂停` 下木材、金币和补给条保持不自增；恢复后继续变化。
- waiting 帧：满手且补给完成时显示满格与 `待入手：电磁塔 · 腾出手牌`；右侧只有一个 `拆除` 情境按钮。
- 交互帧：可用基地牌点击两次后显示 `已取消选择`；按钮在无建筑选择时为 `弃牌`，进入弃牌模式后为 `取消`。
- 360px 帧：木材三位数、金币、产速、补给名称 / 时间、轨道和单一情境按钮均未重叠或裁切。
- 浏览器控制台：error / warn 均为空。

硬锁卡面使用 `hardBlocked=true` 的锁形遮罩与“已达上限 / 暂无合法目标”短文案；其支付 progress 为 `null`，对应路径由 core selector 单测覆盖，避免把硬锁误画成 0% 资源不足。
